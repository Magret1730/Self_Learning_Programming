let x;

// x = 5 + '5'; // turned the number to string and concatenated the result to 55
// x = 5 + Number(5);

// x = 5 * '5'; // turned string to number and multiplied the result

// x = Number(true); // turned true to 1
// x = Number(false); // turned false to 0
// x = 5 + true; // turned true to 1
// x = 5 + false; // turned false to 0

// x = 5 + null; // turned null to 0
// x = Number(null); // turned null to 0
// x = 5 + undefined; // turned undefined to NaN

console.log(x, typeof x);