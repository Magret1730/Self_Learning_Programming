// This is my first javascript comment

// command + forward slash key will add comment (//) to the line of code.
// For multiple lines, shift + up or down/left or right arrow key and the command + forward slash key will add comment to all highlighted lines of code.

/*
Multi
Line
Comment
can be like this.
*/

// command + right/left = takes cursor to the rightmost/leftmost part of that line of code

// for moving cursor per words = option + right/left key

// shift + ctrl + right/left = highlights text faster to the right/left

// to move line of code up/down = option + up/down

// to duplicate = shift + opt + up/down

// undo = command + z

// to get a particular word, highlight the word and then hit command + D

// to get everyhting at once, highlight the word and then hit shift + command + L

// option + click = allows you put your cursor at various places you clicked at the same time. and then type in where all the cursors are.

// command + shift + O = helps search for files

// command + option + F = search for word in the file

// command + shift + F = searches for word in every file in the explorer section.

// command + B = toggle side bar

// control + ~ = toggle terminal

// command + option + I = to bring out console on chrome

// clg + enter = console.log();

console.log(100);

console.log("Hello World");

console.log(100, true, "Hello World");

const x = 100;

console.log(x);

console.log(false);

console.error('Alert');

console.warn('Warn');

console.table({ name: 'Magret', email: 'magret@gmail.com' });

console.group('simple');
console.log(x);
console.log(false);
console.error('Alert');
console.warn('Warn');
console.groupEnd();

const styles = 'padding: 10px; background-color: white; color: green';

console.log('%cHello World', styles);